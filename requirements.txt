# Версии закреплены точно (==), чтобы пересборка образа через полгода дала
# ровно то же, что сегодня. Проверенный набор на 2026-08.
#
# Обновлять по одному пакету за раз и после каждого — прогонять на одном ролике.
# yt-dlp обновляется отдельно и часто (см. scripts/update.sh): площадки ломают
# парсинг заметно чаще, чем выходят релизы остальных зависимостей.

# --- Telegram ---------------------------------------------------------------
# aiogram 3.30 требует Python >=3.10,<3.15 — в образе стоит 3.12.
aiogram==3.30.0
aiohttp==3.14.3

# --- Конфигурация и логирование ---------------------------------------------
pydantic==2.13.4
PyYAML==6.0.3
structlog==26.1.0

# --- Хранилище и сеть --------------------------------------------------------
aiosqlite==0.22.1
httpx==0.28.1

# --- Скачивание видео --------------------------------------------------------
yt-dlp==2026.7.4
